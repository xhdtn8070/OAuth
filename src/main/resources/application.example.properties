spring.datasource.url = jdbc:postgresql://localhost:5432/TEST?serverTimezone=Asia/Seoul
spring.datasource.driver-class-name = org.postgresql.Driver
spring.datasource.platform=postgresql
spring.datasource.username = test
spring.datasource.password = test
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.maximum-pool-size=12
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=1200000
spring.datasource.hikari.auto-commit=true


spring.flyway.url = jdbc:postgresql://localhost:5432/TEST?serverTimezone=Asia/Seoul
spring.flyway.baselineOnMigrate = true
spring.flyway.user = test
spring.flyway.password = test
spring.flyway.encoding = UTF-8
spring.flyway.enabled = true

mybatis.mapper-locations=classpath:xml/*.xml

spring.jpa.hibernate.ddl-auto = validate
spring.jpa.generate-ddl = false
spring.jpa.show-sql = true

spring.redis.host = localhost
spring.redis.port = 6379
spring.redis.lettuce.pool.max-active=10
spring.redis.lettuce.pool.max-idle=10
spring.redis.lettuce.pool.min-idle=2

server.port=80

s3.access-key = a
s3.secret-key = a
s3.bucket = a
s3.custom-domain = a

ses.access-key = a
ses.secret-key = a+a
ses.domain = a.a

project.subDomain = boot.
project.title=TIKIM BOOT API
project.version=Beta 1.0.0
project.ipBandWidth=1
project.school-email=koreatech.ac.kr

token.user.lifetime=60
token.user.name = Authorization
token.csrf.name = X-CSRF-TOKEN

#spring.servlet.multipart.enabled: 멀티파트 업로드 지원여부 (default: true)
#spring.servlet.multipart.file-size-threshold: 파일이 메모리에 기록되는 임계값 (default: 0B)
#spring.servlet.multipart.location: 업로드된 파일의 임시 저장 공간
#spring.servlet.multipart.max-file-size: 파일의 최대 사이즈 (default: 1MB)
#spring.servlet.multipart.max-request-size: 요청의 최대 사이즈 (default: 10MB)

spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML
spring.thymeleaf.encoding=UTF-8
spring.thymeleaf.check-template-location=true
spring.thymeleaf.cache=false

slack.nofity-error-url=https://hooks.slack.com/services/a/a/a

jwt.access-token.time = 72
jwt.refresh-token.time = 336